# Active Profile (defaults to dev for local H2 file database)
spring.profiles.active=dev

# Server Configuration
server.port=8080

# Thymeleaf Cache (disable for development)
spring.thymeleaf.cache=false
